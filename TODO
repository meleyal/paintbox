TODO
----


* rails upgrade checks (rake -T)

* commit

* tag rails 2 (prev rev)

* signup + settings form, error messages

* clean up stylesheet







* cross-browser check, CSS3

* validate format of usernames (no dots)

* set color picker value when click on swatch + focus (copy to clipboard? > flash)

* signup user flow > state for no swatches

* add favorite color to signup

* user admin

* ajax create swatch + animate
    success: add swatch, show success message
    error: show error message
    research: rails3 ajax jquery

* display preferences
    circle, square, line, background color
    set as cookie?
    load additional stylesheet?
    or change classes?