<% if signed_in? && (@user == current_user) %>
  <%= render 'swatches/form' %>
<% end %>  

<% unless @user.swatches.empty? %>
  <ul id="paintbox">
    <%# render  :partial => 'swatches/swatch', :collection => @swatches, :locals => { :link => false } %>    
    <%= render @swatches %>
  </ul>  
<% end %>