<%= simple_form_for(:session, :url => sessions_path) do |f| %>  
  
  <ol>
    <%= f.input :email %>
    <%= f.input :password %>
    <%= f.button :submit, :value => "Sign in" %> <span class="or">or <%= link_to "sign up", signup_path %></span>
  </ol>

<% end %>